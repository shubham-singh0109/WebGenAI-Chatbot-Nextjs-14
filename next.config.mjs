// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;


// next.config.mjs

const nextConfig = {
    images: {
      domains: ['i.pinimg.com'], // Add the hostname(s) of your external images
    },
  };
  
  export default nextConfig;
  